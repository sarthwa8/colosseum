# Binaries
/colosseum
/bin/
*.exe

# Local runtime data (sqlite, event logs, replays)
/data/
*.db
*.db-*
*.sqlite

# Env / secrets
.env
.env.*

# Frontend source deps. internal/web/dist IS committed on purpose: go:embed
# resolves at compile time, so the binary must not depend on a Node toolchain.
/web/node_modules/

# OS / editor
.DS_Store
*.swp
