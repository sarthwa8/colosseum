# Input Constraints

- The input is a single line containing only lowercase letters `a-z` and
  digits `0-9`.
- The length is between `0` and `2*10^5` inclusive — **the line may be empty**,
  in which case the answer is `0`.

Any input satisfying these constraints is valid and must be handled correctly,
including the empty string and strings consisting of one repeated character.
