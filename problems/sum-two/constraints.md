# Input Constraints

- The input is exactly one line.
- It contains two integers `a` and `b` separated by a single space.
- `-10^9 <= a, b <= 10^9`.
- No leading/trailing spaces other than the newline terminator.

Any input satisfying these constraints is valid and must be handled correctly.
