# Sum of Two Integers

Read two space-separated integers `a` and `b` from standard input on a single
line. Print their sum `a + b` to standard output.

## Input
A single line containing two integers separated by a space.

## Output
A single line containing one integer: the sum.

## Example
Input:
```
2 3
```
Output:
```
5
```
